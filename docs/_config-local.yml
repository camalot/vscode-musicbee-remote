---
host: "localhost"
port: 4000
url: "http://localhost:4000"
baseurl: "/vscode-musicbee-remote"

# Use the locally installed gem instead of downloading from GitHub.
# remote_theme requires network access and can cause broken CSS locally.
theme: just-the-docs
remote_theme: ~

livereload: true
force_polling: true

detach: false
